export const config = {
    author: {
        name: "Hernan",
        lastname: "Vera Andino"
    },
    meliApi: {
        baseURL: process.env.MELI_API,
        region: process.env.MELI_SITE,
        limit: process.env.MELI_LIMIT
    }
}